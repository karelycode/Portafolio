import { Poppins } from "next/font/google";

export const titleFont = Poppins({
  subsets: ["latin"],
  weight: ["600", "500"],
});
export const bodyFont = Poppins({
  subsets: ["latin"],
  weight: ["400", "300"],
});
